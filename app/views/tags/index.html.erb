<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li><%= tag.name %><%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Are you sure?"}  %></li>
  <% end %>
</ul>
<%= link_to "Back to Articles list", articles_path %>
